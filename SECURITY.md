# Security

Conformance inputs are hostile data. TCLK frames are accepted as commitments only after exact Technocore signature verification and transport-party matching. Signed bytes are never normalized after verification. Protocol validity and transport representability are reported separately.

The default suites are offline and contain no production key material. Submit vulnerabilities privately to the repository maintainer.
